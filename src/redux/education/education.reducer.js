import EDUCATION_DATA from "./education.data";

const INITIAL_STATE = EDUCATION_DATA;

const educationReducer = (state = INITIAL_STATE, action) => {
  return state;
};

export default educationReducer;
